spring:
  application:
    name: fraudguard

  datasource:
    url: jdbc:postgresql://localhost:5432/fraudguard
    username: postgres
    password: "032523"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true